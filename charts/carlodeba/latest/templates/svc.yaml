apiVersion: v1
kind: Service
metadata:
  name: {{ template "carlodeba" . }}
  labels:
    app: {{ template "carlodeba" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  type: {{ .Values.serviceType }}
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    app: {{ template "carlodeba" . }}

